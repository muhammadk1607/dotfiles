# Packages installed with `apt`. Blank lines and # comments are ignored.
# Verified available on Pop!_OS / Ubuntu 24.04 (noble).

## Build toolchain — also covers what mise needs to compile Ruby and Python
build-essential
gcc
make
pkg-config
autoconf
libssl-dev
libreadline-dev
zlib1g-dev
libsqlite3-dev
libyaml-dev
libffi-dev
libbz2-dev
liblzma-dev
libncurses-dev
libxml2-dev
libxslt1-dev
uuid-dev
tk-dev
curl
wget
git
unzip
gnupg
ca-certificates

## Python — system interpreter only; project runtimes come from mise.
## 24.04 marks this install PEP 668 externally-managed, so tools go via pipx.
python3
python3-dev
python3-venv
python3-pip
pipx
python-is-python3

## Java — 24.04 ships 21 as the default LTS (was 17)
openjdk-21-jdk
openjdk-21-jre

## Shell and CLI
bat
eza
fd-find
ripgrep
zoxide
direnv
httpie
jq
tree
hyperfine
shellcheck
bash-completion

## Clipboard — the session is Wayland, so wl-clipboard replaces xclip.
## xclip stays for the occasional XWayland-only tool.
wl-clipboard
xclip

## Media
ffmpeg
webp
libmagickwand-dev

## Android / mobile
adb
fastboot

## Hardware and system
blueman
net-tools
